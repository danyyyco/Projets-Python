"""words = ["Hello", "Neverland", "World", "How", "Matrix", "Are", "Really", "You", "?"]

for word in words:
    if len(word) > 5:
        print(word)
    else:
        continue
    if 'x' in word:
        break"""

